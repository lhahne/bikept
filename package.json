{
  "name": "bikept-zwift-generator",
  "version": "1.0.0",
  "description": "LLM-powered Zwift workout generator with web UI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && find src -name \"*.js\" -print0 | xargs -0 -n1 node --check && node --check public/app.js",
    "test": "vitest run --globals"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "supertest": "^7.0.0",
    "vitest": "^2.1.1"
  }
}
